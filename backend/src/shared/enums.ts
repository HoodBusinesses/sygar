// Enum for National Identifier Types
export enum NationIdentifierTypes {
  CIN = 'CIN',
  PASSPORT = 'PASSPORT',
  PERMIS = 'PERMIS'
}

// Enum for User Roles
export enum Roles {
  USER = 'USER',
  ADMIN = 'ADMIN',
  SUPER_ADMIN = 'SUPER_ADMIN'
}
